@using MooseMus.Models.ViewModels
@model UserHomeViewModel

@{
    ViewBag.Title = "login";
}

        <h2>Courses</h2>

        <ol>
            <li>Listi af námskeiðum</li>
            @foreach (var course in Model.courses)
            {
                @course.name
            }
        </ol>
